package sample;



import DOTS.SimpleLinkedListLines;

import java.awt.*;

public class fill {
    private Object shapeFill;

    public fill(){
    }

    /**
     * Funcion que de una lista accede a los vertices que debe rellenar
     * @param list
     */
    public static void polygon(SimpleLinkedListLines list) {
     int ind=0;
     //Node current= this.head;
     Polygon poly = new Polygon();
     //while(current!=null){
       // int x= list.getNode(ind).gety;
        //int y= list.getNode(ind).getx;
        //poly.addPoint(x,y);
        //ind++;
     }

     }

